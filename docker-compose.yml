version: "3.9"
services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    env_file: .env
    volumes:
      - ./data:/app/data
  frontend:
    image: nginx:alpine
    ports: ["5173:80"]
    volumes:
      - ./frontend:/usr/share/nginx/html
